

const initialState={auth:false,movie:[],series:[],popular:[],mylist:[]}

const reducer=(oldState=initialState,action)=>{
    const {type,payload} = action;
    switch (type) {
        case "":
            
            break;
    
        default:
            break;
    }
}


export default reducer;